import React from 'react';

const Rules: React.FC = () => {
	return (
		<div className="rules">
			<div className="container">
				<span className="rules__info">
					Get points by clicking on an image but don't click on any more than once!
				</span>
			</div>
		</div>
	);
};
export default Rules;
